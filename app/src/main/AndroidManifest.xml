<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.collegeapplicationsystem">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:ignore="GoogleAppIndexingWarning">
        <activity
            android:name=".UpdateDBPopupActivity"
            android:theme="@style/AppTheme.UpdateDBPopup" />
        <activity
            android:name=".SearchByStatePopupActivity"
            android:theme="@style/AppTheme.EditAccountPopup" />
        <activity
            android:name=".ViewCollegeDetailsActivity"
            android:theme="@style/AppTheme.EditAccountPopup" />
        <activity android:name=".CollegeSearchActivity" />
        <activity
            android:name=".EditReadingScorePopupActivity"
            android:theme="@style/AppTheme.EditAccountPopup" />
        <activity
            android:name=".EditMathScorePopupActivity"
            android:theme="@style/AppTheme.EditAccountPopup" />
        <activity
            android:name=".EditLastNamePopupActivity"
            android:theme="@style/AppTheme.EditAccountPopup" />
        <activity
            android:name=".EditFirstNamePopupActivity"
            android:theme="@style/AppTheme.EditAccountPopup" />
        <activity android:name=".CollegeRealTimeNameSearchActivity" />
        <activity android:name=".ViewAccountActivity" />
        <activity android:name=".MainSearchMenuActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>